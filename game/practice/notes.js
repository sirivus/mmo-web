    //class or parent
class asd {

    //constructor is just another method
  construct() {}
    // methods
  draw() {
      //properties
    c.drawImage(this.image, this.position.x, this.position.y);
  }

  update() {
    this.draw();
  }
}
// with classes, the code is much cleaner and easier to read. and it's easier to debug.
// with classes, you can also use inheritance.
// can define functions inside a class.
// can use this.super() to call the parent class.
// can use super.method() to call the parent class's method.
// can use super.property to call the parent class's property.
// can use super.property = value to set the parent class's property.

function asd() {
  
  debugger;
  
} setInterval(asd, 10);