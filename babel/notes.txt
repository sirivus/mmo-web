npm run build
check package.json for scripts to build or other.

installed
https://playwright.dev/docs/intro#installation
npm init playwright@latest
now playwright is working in my project, imports.

npm-init
Create a package.json file

playwright only tests, not production.
name file with extension spec.ts in folder tests, to test files.

so far, using babel build, actually, can just to build all files into require/exports,
 just compiles into older version of javascript. \
 nvm need to build with browserify
babel is just a compiler

use tsc compiler for typrscript to javascript.
success. \
transcript compiled into javascript
enter into terminal directory, tsc src/helloworld.ts