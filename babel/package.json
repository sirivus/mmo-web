{
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "build": "npm run clean; babel ./src --out-dir ./dist --copy-files",
    "prepublish": "npm run build",
    "start": "node ./dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.21.1"
  }
}
